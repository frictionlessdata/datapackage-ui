window = {};

var _ = require('underscore');
var assert = require('chai').assert;
var should = require('chai').should();


describe('UI Confirmation Dialog box', function() {
  it('define Yes/No event handlers', function(done, err) {
    if (err) done(err);
    assert.fail();
  });

  it('call .yes/.no callbacks in Yes/No event handlers', function(done, err) {
    if (err) done(err);
    assert.fail();
  });
});